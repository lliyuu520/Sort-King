# Design System

> Sort-King UI 设计规范
>
> 本文档用于统一游戏 UI 的视觉与交互表现。
>
> 所有新增或修改的 UI，必须遵循本文档。

---

## 1. Design Principles

Sort-King 的 UI 设计遵循以下原则：

### 1.1 简单

UI 不应增加玩家理解成本。

### 1.2 清晰

玩家应能够快速识别：

- 当前是什么状态
- 当前可以做什么
- 下一步应该做什么

### 1.3 统一

相同功能必须使用相同的视觉表现。

### 1.4 轻量

UI 不抢夺游戏主体的注意力。

### 1.5 友好

整体视觉应保持：

- 轻松
- 明快
- 圆润
- 亲和
- 有趣

### 1.6 核心原则

> UI 是游戏体验的辅助，不是游戏体验的主体。

---

# 2. Design Tokens

所有 UI 应尽可能使用统一的 Design Token。

---

## 2.1 Color

UI 至少包含以下颜色角色：

```text
Primary
Secondary
Background
Surface
TextPrimary
TextSecondary
TextDisabled
Border
Success
Warning
Error
```

### 使用原则

`Primary`

用于：

- 主要按钮
- 主要操作
- 关键状态

`Secondary`

用于：

- 次级操作
- 辅助元素

`Background`

用于：

- 页面背景

`Surface`

用于：

- 卡片
- 面板
- 弹窗

`TextPrimary`

用于：

- 标题
- 核心文字
- 重要数字

`TextSecondary`

用于：

- 辅助说明
- 次要信息

`TextDisabled`

用于：

- 不可操作状态

`Success`

用于：

- 成功
- 完成
- 正确反馈

`Warning`

用于：

- 警告
- 注意

`Error`

用于：

- 错误
- 失败
- 异常

### 规则

禁止单独为某个 UI 随意创造颜色。

新增颜色必须能够归入已有颜色角色。

---

# 3. Typography

UI 使用统一的文字层级。

建议定义：

```text
Display
Title
Subtitle
Body
Caption
Button
Number
```

---

## 3.1 Display

用于：

- 游戏标题
- 特别重要的数字
- 主要结果

特点：

- 最大字号
- 高识别度
- 强视觉存在感

---

## 3.2 Title

用于：

- 页面标题
- 弹窗标题
- 重要区域标题

---

## 3.3 Subtitle

用于：

- 次级标题
- 分类说明

---

## 3.4 Body

用于：

- 普通说明
- 正文

---

## 3.5 Caption

用于：

- 辅助说明
- 次要信息

---

## 3.6 Button

按钮文字必须：

- 清晰
- 简短
- 易识别

例如：

```text
开始
继续
下一关
重玩
设置
返回
确认
取消
```

避免过长文案。

---

## 3.7 Number

游戏中的重要数字：

```text
Score
Combo
Level
Progress
Count
```

可以使用独立的数字样式。

数字必须：

- 清晰
- 易读
- 与普通正文形成明显层级

---

# 4. Spacing

统一使用间距体系。

基础间距：

```text
4
8
12
16
20
24
32
40
48
64
```

优先使用上述数值。

### 推荐用途

```text
4   → 极小间距
8   → 紧凑元素
12  → 组件内部
16  → 普通组件间距
20  → 内容间距
24  → 区域间距
32  → 大区域间距
48  → 页面级间距
64  → 大型视觉间距
```

禁止为了微调而大量产生不规则间距。

---

# 5. Corner Radius

整体 UI 使用圆润风格。

建议：

```text
Small   = 8
Medium  = 12
Large   = 16
XLarge  = 20
Round   = 999
```

推荐：

```text
按钮       → 12~16
卡片       → 16
弹窗       → 20
标签       → 999
圆形按钮   → 999
```

同一页面应保持圆角体系一致。

---

# 6. Shadow

阴影用于表达层级。

推荐层级：

```text
普通组件 → 弱阴影
浮动组件 → 中阴影
弹窗     → 强阴影
```

阴影必须柔和。

禁止：

- 硬边黑色阴影
- 过重阴影
- 大面积发光
- 多层复杂阴影
- 阴影成为视觉主体

---

# 7. Layout

UI 布局采用统一的空间体系。

基本结构：

```text
┌────────────────────┐
│                    │
│     Safe Area      │
│                    │
├────────────────────┤
│       Header       │
├────────────────────┤
│                    │
│                    │
│     Main Content   │
│                    │
│                    │
├────────────────────┤
│    Bottom Area     │
│                    │
└────────────────────┘
```

---

## 7.1 Safe Area

UI 不得紧贴：

- 屏幕顶部
- 刘海区域
- 系统状态区域
- 屏幕底部手势区域

必须保证主要 UI 位于安全区域内。

---

## 7.2 内容区域

页面必须保留合理的边距。

避免：

```text
文字贴边
按钮贴边
卡片贴边
重要信息贴边
```

---

## 7.3 视觉重心

核心 UI 应位于玩家容易注意的位置。

不要为了装饰把重要操作放到边缘。

---

# 8. UI Layer

UI 层级保持清晰：

```text
Background
    ↓
Content
    ↓
HUD
    ↓
Overlay
    ↓
Dialog
    ↓
Toast
```

高层 UI 不应被低层 UI 遮挡。

例如：

```text
Dialog
```

出现时，应位于游戏内容之上。

---

# 9. Button

按钮是游戏最重要的交互组件之一。

统一分为：

```text
Primary
Secondary
Icon
```

---

## 9.1 Primary Button

用于当前页面最重要的操作。

例如：

```text
开始游戏
继续
下一关
```

特点：

- 视觉最突出
- 容易点击
- 文案简短
- 与其他按钮有明显层级

一个页面原则上只有一个主要 CTA。

---

## 9.2 Secondary Button

用于次级操作。

例如：

```text
设置
返回
重玩
取消
```

视觉权重低于 Primary。

---

## 9.3 Icon Button

用于简单操作。

例如：

```text
关闭
返回
设置
声音
```

图标必须容易理解。

---

# 10. Button States

按钮至少需要：

```text
Normal
Pressed
Disabled
Selected
```

必要时支持：

```text
Loading
```

---

## 10.1 Normal

默认状态。

---

## 10.2 Pressed

玩家按下按钮时产生明确但轻微的视觉反馈。

例如：

- 轻微缩放
- 轻微位移
- 阴影变化

---

## 10.3 Disabled

必须明显区别于 Normal。

但不要让 Disabled 变得过度刺眼。

---

# 11. Card

Card 用于承载独立信息。

例如：

- 结果信息
- 设置项
- 功能入口
- 状态信息

统一使用：

```text
Background
Padding
Radius
Shadow
Spacing
```

同一个页面中的 Card 应保持一致。

---

# 12. Dialog

Dialog 用于需要玩家注意或确认的内容。

基本结构：

```text
Overlay
    ↓
Dialog
    ↓
Title
    ↓
Content
    ↓
Actions
```

---

## 12.1 Dialog 原则

Dialog 必须：

- 清晰
- 集中
- 简短
- 容易关闭
- 不遮挡不必要的信息

不要把普通提示全部做成 Dialog。

---

# 13. Toast

Toast 用于短暂反馈。

适用于：

```text
操作成功
保存成功
操作失败
暂不可用
```

Toast 文案应尽可能短。

例如：

```text
保存成功
操作成功
再试一次
```

不适合承载：

- 长篇说明
- 复杂操作
- 重要选择
- 多步骤内容

---

# 14. Badge

Badge 用于：

- 数量
- 状态
- 标签
- 提醒

例如：

```text
NEW
x3
完成
```

Badge 文案必须简短。

---

# 15. Progress

Progress 用于表达明确的进度。

例如：

```text
关卡进度
任务进度
收集进度
```

进度必须容易理解。

避免：

- 复杂装饰
- 无意义动画
- 玩家无法理解的进度表现

---

# 16. Header

顶部区域采用统一结构：

```text
[Left]        [Title]        [Right]
```

例如：

```text
[返回]         游戏          [设置]
```

没有实际功能时，不应为了视觉对称强行放置按钮。

---

# 17. Game HUD

游戏过程中只显示必要信息。

优先显示：

```text
Score
Progress
Pause
```

原则：

> 能不显示，就不显示。

HUD 不应该遮挡：

- 核心游戏区域
- 主要操作区域
- 重要游戏对象

---

# 18. Result UI

结算界面重点突出：

```text
Result
Score
Primary Action
```

推荐结构：

```text
完成

100

[下一关]

[重玩]
```

视觉层级：

```text
结果
 ↓
分数
 ↓
主要操作
 ↓
次要操作
```

---

# 19. Settings UI

设置页面保持简单。

设置项采用统一结构：

```text
Title                  Control
Description            Control
```

例如：

```text
音乐                    ON
音效                    ON
震动                    OFF
```

同类设置项必须保持统一。

---

# 20. Icon

所有 Icon 应保持统一视觉语言。

需要统一：

- 尺寸
- 视觉重量
- 线条粗细
- 圆角
- 风格

禁止在同一界面随意混合完全不同风格的 Icon。

---

# 21. UI Animation

UI 动画必须服务于：

```text
反馈
状态变化
层级变化
引导
```

而不是单纯装饰。

---

## 21.1 Appearance

出现可以使用：

```text
Fade In
Scale In
```

动画应快速完成。

---

## 21.2 Disappearance

消失可以使用：

```text
Fade Out
Scale Out
```

---

## 21.3 Interaction

按钮按下可以使用：

```text
轻微缩放
轻微位移
```

不要产生明显跳动。

---

## 21.4 Animation Principles

动画必须：

- 快
- 轻
- 明确
- 有反馈感

禁止：

- 长时间动画
- 大幅旋转
- 无限循环装饰
- 大量 UI 同时运动
- 为动画而动画

---

# 22. Responsive Design

UI 必须适配不同竖屏设备。

必须考虑：

- 屏幕宽高比例
- 安全区域
- 顶部区域
- 底部区域
- 内容区域

禁止假设所有设备拥有完全相同的尺寸。

---

# 23. UI Consistency

新增 UI 时，应优先复用已有设计。

检查：

```text
颜色是否统一？
字体是否统一？
字号是否统一？
间距是否统一？
圆角是否统一？
阴影是否统一？
按钮是否统一？
Icon 是否统一？
层级是否统一？
```

如果已有组件能够满足需求，不应重新设计一个视觉相同但实现不同的组件。

---

# 24. Visual Hierarchy

任何页面必须存在清晰的视觉层级。

推荐：

```text
主要信息
    ↓
次要信息
    ↓
辅助信息
```

玩家第一眼应该知道：

> 当前页面最重要的是什么？

第二眼知道：

> 我下一步应该做什么？

---

# 25. UI Density

小游戏 UI 应保持适中的信息密度。

避免：

```text
大量文字
大量按钮
大量 Badge
大量图标
大量装饰
```

尤其避免为了“看起来丰富”而增加无意义 UI。

---

# 26. Empty Space

留白是 UI 的组成部分。

不要为了填满屏幕而增加：

- 装饰
- 文字
- 图标
- 按钮
- 卡片

合理留白可以提高：

- 可读性
- 视觉层级
- 操作准确性
- 整体高级感

---

# 27. UI Feedback

玩家进行操作后，应尽可能获得明确反馈。

反馈可以来自：

```text
视觉
动画
数字变化
状态变化
```

但反馈必须轻量。

不要为了增强反馈而同时使用大量效果。

---

# 28. Accessibility

UI 必须保证基本可识别性。

尤其注意：

- 文字与背景具有足够对比度
- 按钮具有明确边界
- Disabled 状态明显
- Icon 不应成为唯一信息来源
- 重要状态不要只依赖颜色区分

---

# 29. UI 禁止事项

禁止：

1. 随意新增颜色
2. 随意新增字体
3. 随意新增字号体系
4. 随意新增圆角体系
5. 随意新增按钮样式
6. 同一个功能使用不同视觉表现
7. 为了装饰增加大量 UI
8. UI 遮挡核心游戏区域
9. 大量使用动画
10. 使用过重阴影
11. 使用过度复杂的渐变
12. 使用无法理解的图标
13. 使用过长 UI 文案
14. 为了填充空间增加无意义内容

---

# 30. New UI Rule

当需要新增 UI 时：

### 第一优先级

复用已有组件。

### 第二优先级

使用已有 Design Token。

### 第三优先级

遵循已有页面布局。

### 第四优先级

只有现有设计无法满足需求时，才增加新的 UI 形式。

新增设计必须与现有系统保持一致。

---

# 31. Final Standard

Sort-King 的 UI 最终应该做到：

```text
看起来统一
        ↓
看起来简单
        ↓
看起来舒服
        ↓
一眼知道怎么操作
        ↓
操作后马上得到反馈
```

最终目标不是让 UI 看起来“复杂”或“高级”。

而是：

> **让玩家几乎不需要学习 UI，就能自然地玩起来。**